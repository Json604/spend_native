<resources xmlns:tools="http://schemas.android.com/tools">
  <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="android:windowOptOutEdgeToEdgeEnforcement" tools:targetApi="35">true</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="android:statusBarColor">#070709</item>
    <!-- Theme.AppCompat.DayNight defaults the window background to WHITE in
         light mode. That window is what shows before the first React frame and
         behind any transition that exposes it — the white flash. -->
    <item name="android:windowBackground">#070709</item>
    <item name="android:navigationBarColor">#070709</item>
  </style>
  <style name="Theme.App.SplashScreen" parent="AppTheme">
    <item name="android:windowBackground">@drawable/ic_launcher_background</item>
  </style>
</resources>